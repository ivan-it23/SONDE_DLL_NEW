# Развёртывание и использование SONDE_DLL (с нейросетью)

Актуально на 2026-09-06. Практическое руководство: **куда класть файлы**, **как
устроен поиск зависимостей** и **в каком порядке вызывать API**, чтобы получить
УЭС и нейросетевые параметры зоны проникновения. Все утверждения сверены с кодом
(`platform/NeuroPredictor.cpp`, `api/SondeApi.cpp`, `core/Constants.h`, `platform/neuro_api.h`,
`SONDE_1.0.cpp` теста).

---

## 1. Три обязательных компонента

Для работы нейросетевого расчёта нужны **три** вещи одновременно:

| Компонент | Что это | Откуда берётся |
|---|---|---|
| `SONDE_DLL_1.0.dll` | Расчётная библиотека (наш код) | Сборка проекта `SONDE_DLL_1.0` |
| `NEURO_TEST.dll` | Нейросетевой рантайм (предиктор) | Готовый бинарь (в `runtime/win-*`, корне репо) |
| `neuro-weights\` | Каталог весов с подпапками по типам | Готовые веса (в `runtime/win-*`) |

Если нужен **только** разбор кадров / УЭС по золотому сечению (`get_cal_signal`,
`simmetry`, `calculate_rho`, `get_express_data`) — достаточно одной
`SONDE_DLL_1.0.dll`. `NEURO_TEST.dll` и веса требуются исключительно для
`calculate_true_rho_neuro` (расчёт `rho_p / rho_zp / R_zp`).

---

## 2. ГЛАВНОЕ ПРАВИЛО РАЗМЕЩЕНИЯ

> **`NEURO_TEST.dll` и папка `neuro-weights\` должны лежать в той же папке, что и
> `SONDE_DLL_1.0.dll`.** Не рядом с exe, а рядом с **самой DLL.**

Причина — в коде поиска. `SONDE_DLL_1.0.dll` определяет свой каталог через адрес
собственного модуля, а не через exe:

```cpp
// platform/NeuroPredictor.cpp
GetModuleFileNameA(reinterpret_cast<HMODULE>(&__ImageBase), path, MAX_PATH);
// -> dllDir = каталог именно SONDE_DLL_1.0.dll
LoadLibraryExA(dllDir + "NEURO_TEST.dll", ..., LOAD_LIBRARY_SEARCH_DLL_LOAD_DIR);
BuildWeightsDir(dllDir, toolType, ...);   // ищет dllDir\neuro-weights\...
```

На практике exe (например `SONDE_1.0.exe`) грузит `SONDE_DLL_1.0.dll` рядом с
собой (`LoadSondeLibrary`), поэтому в типовом случае **всё лежит в одной папке**:

```
<папка приложения>\
├── SONDE_1.0.exe              (или ваше приложение-потребитель)
├── SONDE_DLL_1.0.dll          ← exe грузит эту DLL
├── NEURO_TEST.dll             ← SONDE_DLL грузит рядом с собой
└── neuro-weights\             ← SONDE_DLL ищет рядом с собой
    ├── LWD_4Tx-241\           (12 файлов .bin)
    ├── LWD_4Tx_NEW-242\       (12 файлов .bin)
    └── CARTOGRAPH_LWD_4Tx-349\(12 файлов .bin)
```

Цепочка загрузки:

```
приложение  ──LoadLibrary──►  SONDE_DLL_1.0.dll  ──LoadLibraryEx──►  NEURO_TEST.dll
                                     │
                                     └── ищет neuro-weights\*-<код> рядом с собой
```

---

## 3. Разрядность (x64 / x86) — не смешивать

Все три компонента и приложение **обязаны быть одной разрядности**. 64-битный
процесс не загрузит 32-битную DLL (Win32 error 193 «%1 не является приложением
Win32») и наоборот.

| Разрядность | `SONDE_DLL_1.0.dll` | `NEURO_TEST.dll` |
|---|---|---|
| **x64** | `SONDE_DLL_NEW\x64\Release\` | `runtime\win-x64\` (1 380 352 б) |
| **Win32 (x86)** | `SONDE_DLL_NEW\Release\` | `runtime\win-Win32\` (1 110 528 б) |

Веса `neuro-weights\` — платформонезависимы (обычные `.bin`), но продублированы в
`runtime\win-x64` и `runtime\win-Win32` для удобного комплектования.

---

## 4. Куда что класть — пошагово

### 4.1. Готовый бандл зависимостей

`runtime\win-x64\` и `runtime\win-Win32\` — это **комплекты нейро-зависимостей**
под соответствующую разрядность: в каждом лежат `NEURO_TEST.dll` + `neuro-weights\`.
Свою `SONDE_DLL_1.0.dll` вы добавляете туда из вывода сборки нужной платформы.

### 4.2. Развёртывание рядом с приложением (production)

1. Определите разрядность приложения (x64 или x86).
2. В папку приложения положите:
   - `SONDE_DLL_1.0.dll` — из `SONDE_DLL_NEW\x64\Release\` (x64) либо
     `SONDE_DLL_NEW\Release\` (x86);
   - `NEURO_TEST.dll` — из `runtime\win-x64\` (x64) либо `runtime\win-Win32\` (x86);
   - папку `neuro-weights\` целиком — из `runtime\win-x64\` или `runtime\win-Win32\`.
3. Убедитесь, что все три — одной разрядности с приложением (см. §3).

### 4.3. Тестовый проект (`SONDE_DLL_TEST_NEW`)

Пост-билд-шаг теста (`SONDE_1.0.vcxproj`) **сам** копирует свежую
`SONDE_DLL_1.0.dll`, `NEURO_TEST.dll` и `neuro-weights\` из вывода DLL-проекта
рядом с `SONDE_1.0.exe`. Поэтому достаточно:

1. Собрать проект `SONDE_DLL_1.0` в нужной платформе (`Release|x64` и/или `Release|x86`).
2. Собрать (или пересобрать) проект теста `SONDE_1.0` **в той же платформе** — он
   подтянет актуальную DLL и веса.

> ⚠️ Если пересобрать только DLL-проект, а тест — нет, рядом с `SONDE_1.0.exe`
> останется **старая** `SONDE_DLL_1.0.dll`. Всегда пересобирайте и тест (или
> копируйте DLL вручную по разрядности).

Порядок поиска DLL в тесте (`LoadSondeLibrary`): (1) рядом с exe → (2) рабочая
папка → (3) захардкоженный путь `C:\WORK\repos\...` → (4) `..\Debug\`. В норме
срабатывает первый.

---

## 5. Структура `neuro-weights\` и правило выбора

### 5.1. Именование подпапок

Имя подпапки — `<произвольное_имя>-<XYZ>`, где завершающие **3 цифры `XYZ`** —
код прибора:

- `X` — тип/семейство (`2` = LWD, `3` = картограф в режиме LWD);
- `Y` — число передатчиков (`4` для нейро-приборов);
- `Z` — **модификация**.

Часть-имя до дефиса произвольна (`LWD_4Tx`, `CARTOGRAPH_LWD_4Tx` — для читаемости).
Значение имеет только завершающий `-XYZ`.

### 5.2. 12 обязательных файлов в каждой подпапке

```
w1.bin  w2.bin  w3.bin  w4.bin      (веса слоёв)
b1.bin  b2.bin  b3.bin  b4.bin      (смещения слоёв)
in_mean.bin  in_scale.bin           (нормировка входа)
out_min.bin  out_scale.bin          (денормировка выхода)
```

Подпапка, где не хватает хотя бы одного файла, считается **неполной** и
пропускается (в логе — `incomplete candidate: ... (missing ...)`).

### 5.3. Правило выбора весов (важно)

Модификация (`Z`) **не влияет** на выбор. `BuildWeightsDir`:

1. Обязательный матч — по первым двум цифрам (тип `X` + число передатчиков `Y`),
   которые берутся из сигнатуры прибора в файле данных.
2. Если есть полная подпапка с **точным** совпадением всех трёх цифр — берётся она.
3. Если точной модификации нет — берётся любая полная подпапка с тем же `XY`
   (fallback). Модификация не блокирует загрузку.

Примеры на текущем наборе:
- картограф `34Z` при любой `Z` → `CARTOGRAPH_LWD_4Tx-349` (единственная `34*`);
- `241` → `LWD_4Tx-241`, `242` → `LWD_4Tx_NEW-242` (точная модификация разрешает
  единственную коллизию внутри `24`).

Допуск к нейросети в целом: `N_Tx == 4 && (type_ == 2 || type_ == 3)`.

---

## 6. Использование API — порядок вызовов

Все функции — `extern "C"`, возвращают `int` (0 = успех), кроме
`sonde_get_last_error` и `debug_mode`. Подробности каждой — в §5.2 главного
`DOCUMENTATION.md`. Ниже — рабочая последовательность для нейро-расчёта.

### Шаг 0 (опц.) — включить лог

```c
debug_mode(true);   // пишет Test.txt рядом с рабочей папкой; для диагностики
```

### Шаг 1 — загрузить метрологию и инициализировать нейросеть

```c
int rc = sonde_set("path\\to\\metro_....bin");   // аргумент — ПУТЬ к файлу .bin (240 байт)
if (rc != 0) { /* см. sonde_get_last_error() и коды §7 */ }
```

`sonde_set` читает метрологию, декодирует сигнатуру, и **если прибор нейро-типа —
сразу инициализирует `NEURO_TEST.dll` и загружает веса**. Именно здесь возникают
ошибки `200..203`, если зависимостей нет на месте.

### Шаг 2 — проверить файл данных

```c
uint32_t frames = 0, sig = 0; int header = 0;
rc = get_data_file_info("path\\to\\data", &frames, &header, &sig);
// frames — число кадров, header — размер служебной части, sig — сигнатура
```

### Шаг 3 — определить размер записи кадра по сигнатуре

`struct_size` кодируется в старших разрядах сигнатуры (`signature / 1000000`):
240 (старая прошивка) или 320 (новая). Читайте `min(struct_size, sizeof(GP_DATA))`
байт в **обнулённую** `GP_DATA` — безопасно для обеих версий.

### Шаг 4 — цикл по кадрам

```c
CAL_SIGNAL cal = {}, smt = {};
RHO ro_phase = {}, ro_neuro = {};
SERVICE svc = {};
uint32_t condition = 0;

for (uint32_t i = 0; i < frames; i++) {
    void* frame = /* указатель на i-й кадр (обнулённая GP_DATA, скопировано struct_size байт) */;

    get_cal_signal(frame, &cal, /*shift*/0);   // калибровка: фазы и затухания (дБ)
    get_condition(frame, &condition, 0);        // байт работоспособности передатчиков
    simmetry(&cal, &smt, condition);            // симметризация обоих каналов матрицей K

    calculate_rho(&smt, &ro_phase);             // УЭС по фазе и по затуханию (все зонды)
    calculate_true_rho_neuro(&smt, &ro_neuro, &svc);  // НЕЙРО: rho_p / rho_zp / R_zp

    // (опц.) get_express_data(frame, &cal, &ro, 0) — готовые сигналы/УЭС с контроллера
    // результаты -> ваши графики/выгрузка
}
```

**Важно про `calculate_true_rho_neuro`:** на вход подаётся **симметризованный**
сигнал (`smt`), а не сырой `cal`. Коррекция за скважину не выполняется.
В `SERVICE` возвращается разброс фазовых УЭС по активным зондам, %.

---

## 7. Нейро-путь детально (что подаётся и что возвращается)

**Вход (8 значений)** формируется в `calculate_true_rho_neuro` из симметризованных фаз
4 зондов × 2 частоты, переведённых в **градусы**:

```
raw_inputs[0..3] = smt.phase[0][0..3] * Grad   // частота 400,  зонды T1..T4
raw_inputs[4..7] = smt.phase[1][0..3] * Grad   // частота 2000, зонды T1..T4
```
(`Grad = 180/π`.)

**Выход (3 значения)** `GeoPredictor_Predict` и маппинг в `RHO`:

| out[i] | Смысл | Куда в `RHO` | Единицы |
|---|---|---|---|
| `out[0]` = r_inv | радиус зоны проникновения | `R_zp` = r_inv·100 | вход м → хранится в **см** |
| `out[1]` = rho_inv | УЭС зоны проникновения | `rho_zp` | Ом·м |
| `out[2]` = rho_form | УЭС пласта | `rho_p` | Ом·м |

`rho_p / rho_zp / R_zp` записываются **одинаково для обеих частот** — это свойства
среды. Значения проходят проверку `isfinite && > 0`, иначе `calculate_true_rho_neuro`
вернёт `300` (`kNeuroPredictFailed`).

Обратная проверка/визуализация модельных фаз для полученных параметров — функция
`ph_smt_zp` (прямая задача, `R_zp` из см обратно в метры).

---

## 8. Диагностика и коды ошибок

Текст последней ошибки — `sonde_get_last_error()` (thread-local). Полный лог —
`debug_mode(true)` → `Test.txt` (строки с префиксом `[NEURO]` — входы/выходы сети).

| Код | Константа | Причина | Что проверить |
|---|---|---|---|
| `200` | `kNeuroDllNotLoaded` | не загрузилась `NEURO_TEST.dll` | лежит ли она рядом с `SONDE_DLL_1.0.dll`; совпадает ли разрядность |
| `201` | `kNeuroFuncNotFound` | нет экспортов `GeoPredictor_*` | версия/битность `NEURO_TEST.dll` |
| `202` | `kNeuroCreateFailed` | предиктор не создан из весов | целостность файлов весов; `GeoPredictor_GetLastError` в логе |
| `203` | `kNeuroWeightsNotFound` | нет весов для типа+N_Tx | есть ли полная подпапка `neuro-weights\*-<XY>?`; все 12 `.bin` |
| `300` | `kNeuroPredictFailed` | сбой предсказания или нефизичный результат | входные фазы; лог `[NEURO]` |
| `301` | `kNeuroNotInitialized` | нейросеть не инициализирована | вызван ли успешно `sonde_set` для нейро-типа |
| `100` | `kUnsupportedType` | тип прибора не поддержан нейросетью | `calculate_true_rho_neuro` только для LWD 4Tx / картографа LWD 4Tx |

Прочие коды (`1..12` — метрология/файл/аргументы/численный сбой) — в `core/Constants.h`
и §5.5 главного `DOCUMENTATION.md`.

---

## 9. Чек-лист развёртывания

- [ ] Разрядность приложения известна (x64 или x86).
- [ ] `SONDE_DLL_1.0.dll` нужной разрядности лежит там, откуда его грузит приложение.
- [ ] `NEURO_TEST.dll` **той же** разрядности — в одной папке с `SONDE_DLL_1.0.dll`.
- [ ] Папка `neuro-weights\` — в одной папке с `SONDE_DLL_1.0.dll`.
- [ ] Нужная подпапка весов присутствует и содержит все 12 `.bin` (тип+N_Tx прибора).
- [ ] `sonde_set` возвращает 0 (иначе — `sonde_get_last_error`).
- [ ] На вход `calculate_true_rho_neuro` подаётся **симметризованный** сигнал.
